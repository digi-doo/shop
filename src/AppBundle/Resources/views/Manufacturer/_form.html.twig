{% from '@SyliusAdmin/Macro/translationForm.html.twig' import translationFormWithSlug %}
{% from '@SyliusAdmin/Macro/singleImageWidget.html.twig' import singleImageWidget %}

{{ form_errors(form) }}

<div class="ui segment">
    <h4 class="ui dividing header">{{ 'sylius.ui.details'|trans }}</h4>
    {{ form_errors(form) }}
    
    {{ form_row(form.enabled) }}
    {{ form_row(form.code) }}
</div>
<div class="ui segment">
    <h4 class="ui dividing header">{{ 'app.ui.display_information'|trans }}</h4>
    {{ form_row(form.filterable) }}
    {{ form_row(form.stockSortingEnabled) }}
</div>
<div class="ui segment">
    <h4 class="ui dividing header">{{ 'app.ui.logo_image'|trans }}</h4>
    {{ singleImageWidget(form.image) }}
</div>
{{ translationFormWithSlug(form.translations, 'AppBundle:Manufacturer:_slugField.html.twig', manufacturer) }}