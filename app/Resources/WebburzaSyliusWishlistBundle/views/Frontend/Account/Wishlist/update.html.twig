{% extends '@SyliusShop/Account/layout.html.twig' %}

{% form_theme form '@SyliusShop/Ui/Form/theme.html.twig' %}

{# {% block breadcrumb %}
    <div class="ui breadcrumb">
        <a href="{{ path('sylius_shop_homepage') }}" class="section">{{ 'sylius.ui.home'|trans }}</a>
        <div class="divider"> / </div>
        <a href="{{ path('sylius_shop_account_dashboard') }}" class="section">{{ 'sylius.ui.my_account'|trans }}</a>
        <div class="divider"> / </div>
        <a href="{{ path('webburza_account_wishlist_index') }}" class="section">{{ 'webburza_wishlist.account.index.header'|trans }}</a>
        <div class="divider"> / </div>
        <div class="active section">{{ 'webburza_wishlist.account.update.header'|trans }}</div>
    </div>
{% endblock %} #}

{% block subcontent %}
    <header class="tmpHeader">
        <h1>{{ 'webburza_wishlist.account.update.header'|trans }} <small>{{ 'webburza_wishlist.account.update.subheader'|trans }}</small></h1>
        <hr>
    </header>
    <div class="ui segment">
        {{ form_start(form, {'action': path('webburza_account_wishlist_update', {id: wishlist.id}), 'attr': {'class': 'ui loadable form', 'novalidate': 'novalidate'}}) }}

        {{ form_errors(form) }}
        {{ form_rest(form) }}

        <button type="submit" class="ui large primary button btn">{{ 'sylius.ui.save_changes'|trans }}</button>
        {{ form_end(form, {'render_rest': false}) }}

    </div>
{% endblock %}
