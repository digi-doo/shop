<div class="ui tab" data-tab="locales">
    <h3 class="ui dividing header">{{ 'sylius.form.channel.locales'|trans }}</h3>    
    <div class="column">
        {{ form_row(form.locales) }}
        {{ form_row(form.defaultLocale) }}
    </div>
    {{ sonata_block_render_event('sylius.admin.channel.' ~ action ~ '.tab_settings', {'form': form}) }}
</div>
